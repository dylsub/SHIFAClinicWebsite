import React from "react";

const TeamItem = () => {
  // Ayush
  // Represents each individual team panel
  // Has a picture that is stored in a div element.
  // Make sure this image does not scaled but instead gets cropped to fit the aspect ratio
  // Use a typically black transparent button for right now but we will change this later

  return <div></div>;
};

export default TeamItem;
